import {
  StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
  container: {
    ...StyleSheet.absoluteFillObject,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff'
  },
  loadingIconView: {
    width: '100%',
    height: '100%'
  },
  loadingIcon: {
    width: '100%',
    height: '100%'
  }
});